[pytest]
asyncio_mode = auto
testpaths = tests
python_files = test_*.py
markers =
    unit: fast, no external deps
    integration: requires test DB
    e2e: requires full app stack
    contract: validates API shapes
    load: performance tests
log_cli = true
log_cli_level = WARNING
filterwarnings =
    ignore::DeprecationWarning
