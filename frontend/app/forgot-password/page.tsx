export default function ForgotPasswordPage() {
  return (
    <main className="mx-auto max-w-md px-6 py-16 text-stone-100">
      Password reset email is not enabled in local auth yet. Sign in and change your password from Profile.
    </main>
  );
}
