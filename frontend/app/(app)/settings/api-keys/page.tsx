export default function ApiKeysPage() {
  return <main className="rounded-xl border border-stone-700 bg-stone-900/70 p-6 text-stone-100">API key management is handled in backend auth, not browser storage.</main>;
}
